# Core dependencies (already installed in llms environment)
torch>=2.7.0  # Compatible with MedSAM2 (requires >=2.5.1)
torchvision>=0.22.0  # Compatible with MedSAM2 (requires >=0.20.1)
numpy>=2.0.1  # Updated for MedSAM2 compatibility
opencv-python>=4.11.0  # Compatible with MedSAM2 (requires >=4.10.0)
Pillow>=11.2.1  # Compatible with MedSAM2 (requires >=10.4.0)

# SAM dependencies
### segment-anything @ git+https://github.com/facebookresearch/segment-anything.git
matplotlib>=3.10.1  # Compatible with MedSAM2 (requires >=3.9.1)
tqdm>=4.67.1  # Compatible with MedSAM2 (requires >=4.66.5)

# MedSAM2 core dependencies
hydra-core>=1.3.2
iopath>=0.1.10
SimpleITK>=2.4.0  # Updated for MedSAM2 compatibility

# MedSAM2 optional dependencies for notebooks and development
eva-decord>=0.6.1
fvcore>=0.1.5.post20221221
tensorboard>=2.17.0
pycocotools>=2.0.8
tensordict>=0.5.0
submitit>=1.5.1

# MedSAM2 interactive demo dependencies (optional)
Flask>=3.0.3
Flask-Cors>=5.0.0
av>=13.0.0
dataclasses-json>=0.6.7
gunicorn>=23.0.0
imagesize>=1.4.1
strawberry-graphql>=0.239.2

# Medical SAM dependencies
transformers>=4.36.0
datasets>=2.0.0
accelerate>=0.20.3
pymatreader>=0.9.5
awscli>=1.32.0  # For downloading Medical Decathlon datasets

# nnU-Net dependencies
nnunetv2 @ git+https://github.com/MIC-DKFZ/nnUNet.git
acvl-utils>=0.2.3,<0.3
dynamic-network-architectures>=0.4.1,<0.5
scipy
batchgenerators>=0.25.1
scikit-learn
scikit-image>=0.24.0  # Updated for MedSAM2 compatibility
pandas>=2.2.3  # Updated for MedSAM2 compatibility
graphviz
tifffile
requests>=2.28.0  # Updated for dataset download compatibility
nibabel>=4.0.0  # Updated for dataset download compatibility
seaborn
imagecodecs
yacs
batchgeneratorsv2>=0.3.0
einops
blosc2>=3.0.0b1

# RL dependencies (to be installed)
gymnasium>=0.29.1
stable-baselines3>=2.2.0
wandb>=0.16.0  # for experiment tracking

# Development dependencies (to be installed)
pytest>=7.4.3
black>=24.2.0  # Compatible with MedSAM2 (requires >=24.2.0)
isort>=5.13.0
flake8>=7.0.0
mypy>=1.8.0

# MedSAM2 development dependencies
usort==1.0.2
ufmt==2.0.0b2

# Jupyter (already installed in llms environment)
jupyter>=1.0.0  # Compatible with MedSAM2 (requires >=1.0.0)
## ipykernel>=6.29.5
## notebook>=7.0.6

# Optional: GPU support
cuda-python  # Uncomment if using NVIDIA GPU
cupy-cuda11x  # Uncomment and replace x with your CUDA version if using NVIDIA GPU 